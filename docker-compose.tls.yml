services:
  frontend:
    ports:
      - "443:443"
    volumes:
      - ./deploy/gcp/nginx-tls.conf:/etc/nginx/conf.d/default.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
      - /var/www/certbot:/var/www/certbot:ro
